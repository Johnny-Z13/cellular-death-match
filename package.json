{
  "name": "cellular-death-match",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "qa:mobile": "node scripts/qa-mobile-hardening.mjs",
    "qa:merge-lab": "node scripts/qa-merge-lab.mjs",
    "preview": "vite preview",
    "generate:audio": "node scripts/generate-audio-assets.mjs",
    "generate:keyart": "node scripts/generate-keyart.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "sharp": "^0.35.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
